import { Bell, CalendarDays, FileHeart, MessageCircleMore } from 'lucide-react'

export const profileMenuIcons = {
  calendar: CalendarDays,
  file: FileHeart,
  message: MessageCircleMore,
  bell: Bell,
}
